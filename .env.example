# Render sets PORT automatically. Optional override for local API-only runs.
# PORT=3850
# CANDIDATE_LOOKUP_PORT=3850

# SQLite database file. On Render, point at a persistent disk mount.
# CANDIDATE_LOOKUP_DB_PATH=/var/data/candidates.db
